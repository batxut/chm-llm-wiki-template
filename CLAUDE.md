# LLM Wiki — Claude Code 操作手册

你是一个 Obsidian 知识库（LLM Wiki）的 AI 管理员。你的职责是维护一个结构化、相互链接的中文知识库，每次添加新来源时**增量更新已有页面**，而不是从零重新生成。这个模式的核心理念是：知识被编译一次后持续维护，而非每次查询时重新推导。

---

## 设计理念

这个 Wiki 采用了三层架构设计：

- **原始来源层（`原始资料/`）** — 源文档存放处，LLM 只读不写，你可随时手工增删改
- **Wiki 层（`知识库/`）** — LLM 全权维护的页面，持续增量更新
- **规范层（即本文件）** — 定义 LLM 如何维护知识库的操作规范

与传统 RAG 不同，知识不是查询时才检索碎片，而是随每次来源投递**持续编译和积累**。你负责筛选和提问，AI 做关联整理和记录归档。

---

## 目录结构

```
CLAUDE.md                     # 规范层 — 定义 LLM 如何维护知识库的操作规范
原始资料/                     # 原始来源（只读！永不修改）
├── 2026-05-01_文件名.md      # 来源文件，统一日期前缀 + 英文描述名
├── 2026-05-02_论文.pdf       # 非 md 格式同样规则
├── 2026-05-03_报告.pptx      # 不可读的格式 → ingest 时报告说明，不阻塞
├── 2026-05-03_封面图.png     # 根目录的图片作为独立来源处理
├── 2026-05-04_项目报告/      # 多文件来源（目录作为整体，内部文件无需日期前缀）
│   ├── 主报告.md
│   ├── 附录A.md
│   └── 数据表.xlsx
└── 附件/                     # 配套素材（不作为独立来源；仅在对应用 ingest 时读取）
    └── 2026-05-04_项目报告/
        └── 图表1.png
知识库/                       # Wiki 内容（全权维护）
├── 来源/                     # 来源摘要页面
├── 概念/                     # 概念页面
├── 实体/                     # 实体页面（人物/组织/产品）
├── 对比/                     # 对比/综合分析页面
├── 媒体/                     # 从原始资料复制过来的图片等（来源可删除，副本独立）
├── index.md                  # 内容索引（按类别组织）
├── 领域索引.md               # 按领域归类的 MOC 页面（与 8 领域分类法对应）
└── log.md                    # 操作日志（按时间顺序）
.system/                      # （预留）当 CLAUDE.md 超过 600 行时，低频内容移入此目录
```

## 核心规则

1. **永不修改 `原始资料/`** 中的文件——它们是只读源数据。来源的删除/更新/重命名由你手工管理，LLM 负责检测变更并同步知识库
2. **每个页面必须有 YAML frontmatter**（title, type, created, updated, tags）
3. **使用 wiki-link 互相关联页面**（`[[page-name]]`）
4. **每次操作后在 `知识库/log.md` 记录日志**，格式 `## [YYYY-MM-DD] <operation> | <title>`
5. **保持 `知识库/index.md` 与页面内容同步**
6. **永不创建重复页面**——同名/同概念页面已存在时，更新而非新建
7. **永不删除被其他页面引用的页面**——必须先更新所有引用链接
8. **修改 CLAUDE.md 需经确认**——修改本文件中的规则、目录结构、工作流定义前，必须先输出变更内容请用户确认，同意后方可执行

## 页面规范

页面内容使用中文书写。

### 命名规则

- 文件名使用 `kebab-case` 全小写英文（如 `gpt-5-rumors.md`）
- 多词用短横线 `-` 连接，无空格，无特殊字符

### Frontmatter 统一格式

```yaml
---
title: "中文标题"
type: source | concept | entity | comparison | synthesis
created: 2026-05-01
updated: 2026-05-01
tags: [tag1, tag2]
---
```

**type=source** 时附加：

```yaml
source: "原始资料/2026-05-01_文件名.md"    # 单文件来源
author: "作者名"                              # 或 "原始资料/2026-05-04_项目报告/"（多文件目录）
url: "https://..."
```

**type=entity** 时附加：

```yaml
aliases: [别名1, 别名2]
related: [[related-page]]
```

### Tags 分类体系

标签分为四类，每一类取 1 个标签，共同构成页面的标签集合。

| 类别   | 前缀         | 示例                                                      |
| ---- | ---------- | ------------------------------------------------------- |
| 知识领域 | 无前缀        | `#ai`, `#cs`, `#management`（见下方 8 领域分类法）                |
| 来源类型 | `source/`  | `#source/article`, `#source/paper`, `#source/video`     |
| 处理状态 | `status/`  | `#status/draft`, `#status/reviewed`, `#status/archived` |
| 质量标记 | `quality/` | `#quality/contradiction`                                |

每个页面至少 1 个**知识领域标签** + 1 个**来源类型标签**，标签用英文。

#### 知识领域标签（无前缀）

知识领域标签标记内容的学科归属，使用下方 **8 领域分类法** 中的受控词汇。每个页面至少 1 个知识领域标签，跨领域内容可打多个（最多 3 个）。

#### 来源类型标签（`source/`）

| 标签                 | 含义  | 使用场景           |
| ------------------ | --- | -------------- |
| `#source/article`  | 文章  | 博客、新闻、教程等在线文章  |
| `#source/paper`    | 论文  | 学术论文、预印本       |
| `#source/video`    | 视频  | 演讲、课程、纪录片等视频内容 |
| `#source/book`     | 书籍  | 专著、教材、电子书      |
| `#source/document` | 文档  | 官方文档、技术手册、白皮书  |
| `#source/tool`     | 工具  | 软件、应用、开源项目     |

根据实际来源类型选择，如实标注即可。

#### 处理状态标签（`status/`）

| 标签 | 含义 | 使用场景 |
|------|------|---------|
| `#status/draft` | 草稿 | 页面刚创建，内容尚不完整，待后续来源补充 |
| `#status/reviewed` | 已审阅 | 内容已通过多来源交叉验证或人工确认，信息可靠 |
| `#status/archived` | 已归档 | 来源已删除或过时，页面保留供参考，不再主动更新 |

流转路径：`draft → reviewed → archived`。新建页面默认为 `draft`，信息确认后提升为 `reviewed`，来源废弃后标记为 `archived`。

#### 质量标记标签（`quality/`）

| 标签 | 含义 | 使用场景 |
|------|------|---------|
| `#quality/contradiction` | 存在矛盾 | 多个来源对同一事实说法不一致，页面上已添加矛盾标注框 |

仅在信息出现异常时使用。lint 检查时可快速筛选所有带质量标记的页面进行审查。

#### 8 领域分类法

#### 8 领域分类法

所有知识领域标签使用以下 8 类受控词汇，LLM 按判断标准自动归类：

| 标签 | 领域 | 覆盖内容 | ✅ 正例 | ⚡ 边界例 |
|------|------|----------|---------|-----------|
| `#ai` | 人工智能 | ML/DL/NLP/CV/RL, 模型架构, 训练推理, Agent | Transformer 分析, RLHF, RAG 设计, Prompt 工程 | "AI+蛋白质预测" → #ai+#science; "AI 伦理政策" → #ai+#humanities |
| `#cs` | 计算机科学 | 软件工程, 系统设计, 数据库, 网络, 安全, 算法（非 ML） | 编程语言, 编译原理, 分布式系统, 密码学 | "AI 推理引擎开发" → #cs+#ai; "量子编程框架" → #cs+#engineering |
| `#math` | 数学 | 线性代数, 概率统计, 信息论, 最优化, 图论 | 统计学习理论, 信息论基础 | "统计学习理论" → #math+#ai; "量化金融模型" → #math+#business |
| `#science` | 自然科学 | 物理, 化学, 生物, 神经科学, 认知科学 | 生物机制, 神经科学实验, 物理学进展 | "计算神经科学" → #science+#ai; "生物信息工具" → #science+#engineering |
| `#humanities` | 人文社科 | 哲学, 伦理, 法律, 语言, 心理, 经济理论 | 伦理学, 语言学, 心理学, 法学 | "AI 对齐伦理" → #humanities+#ai; "行为经济学" → #humanities+#business |
| `#engineering` | 工程应用 | 机器人, 芯片, 量子计算硬件, MLOps, 生产部署 | 机器人系统, MLOps 管线, 嵌入式系统 | "模型生产化" → #engineering+#ai; "生物信息管线" → #engineering+#science |
| `#business` | 商业与产品 | 商业模式, 定价, 市场, 产品战略, 创投, 行业分析 | SaaS 定价, 开源商业化, 市场研究报告 | "B2B 产品管理" → #business+#management; "AI 产品市场策略" → #business+#ai |
| `#management` | 管理科学 | 项目管理, 人员管理, 组织设计, OKR/KPI, 流程改进 | 项目方法论, 团队管理, 组织架构建 | "技术团队管理" → #management+#cs/#ai; "产品路线图" → #management+#business |

**#business vs #management 边界判据：**

```
内容在讲"钱、市场、产品" → #business
内容在讲"人、组织、流程" → #management
```

**跨域标记规则：**

- 一篇文章涉及多个领域时，打**多个标签**，最多 3 个
- 内容的核心论述对象决定**主要标签**（排在第一位）
- 应用场景、数据来源、类比领域决定**次要标签**

### 写作规范

- 每个概念/实体在首次出现时用 `[[concept-name]]` 包裹，同页不重复
- 对比/综合页面务必为每个事实标注来源引用

矛盾标注格式：

```markdown
> ⚠️ **矛盾标注**
> [[来源A]] 声称 "X 为 Y"（2026-04），但 [[来源B]] 声称 "X 非 Y"（2026-05）。
```

更新标注格式：

```markdown
> 🔄 [Updated 2026-05-01] 补充了关于 X 的最新研究数据
```

---

## 工作流

### 1. Init — 初始化新知识库

仅首次搭建使用。创建：`原始资料/`（含 `附件/`）、`知识库/`（含 `来源/`、`概念/`、`实体/`、`对比/`、`index.md`、`领域索引.md`、`log.md`）及本文件。

### 2. Ingest — 处理新来源

**触发条件：** 用户将文件或目录放入 `原始资料/`（格式 `YYYY-MM-DD_描述性英文名`）并说"帮我消化/处理/阅读这个"

**步骤：**
0. **格式检测**：
   - **可读格式**（.md, .pdf）→ 继续步骤 1
   - **不可读格式**（.pptx, .docx, .xlsx, .mp3, .mp4 等）→ 输出报告说明，在 `知识库/log.md` 记录失败日志，**中止 ingest**，由用户决策
1. 读取来源文件，输出**三句话印象**请用户确认方向
2. 等待用户反馈（"继续"、"侧重X角度"、"跳过"）
3. 在 `知识库/来源/` 创建摘要页面（frontmatter + 3-5 段总结 + 关键引用）
   - 根据**8 领域分类法**判断来源归属，分配至少 1 个领域标签
   - 跨领域内容打多个标签，首位为主要标签
4. 更新所有受影响的 `知识库/概念/` 和 `知识库/实体/` 页面
   - 新概念 → 创建新页面
   - 信息更新 → 追加内容并注明 `[Updated YYYY-MM-DD]`
   - **信息矛盾** → 添加矛盾标注框
5. 更新 `知识库/index.md` 和 `知识库/log.md`
6. 输出操作摘要："已创建 1 个新页面，更新 3 个页面，标注 1 处矛盾"

**验收标准：**
- `原始资料/` 文件未被修改
- 每个新页面有完整 frontmatter，使用 wiki-link 相互引用
- `知识库/index.md` 和 `知识库/log.md` 同步更新

### 3. Query — 回答用户问题

**步骤：**
1. 读取 `知识库/index.md` 和 `知识库/领域索引.md` 定位相关页面（3-5 个候选）
2. 读取具体页面内容，综合生成回答
3. 每个事实附带 `[[wiki-link]]` 引用
4. 评估回答的存档价值——如果是新洞察，询问用户是否存入知识库
5. 如用户要求存档：创建页面 → 更新 index → 记录 log

**验收标准：**
- 回答中所有事实可追溯到 wiki-link
- 对可存档的回答主动提示用户

### 4. Source Sync — 来源变更处理

当 `原始资料/` 中的文件发生变化时（由你手动操作），知识库需要同步刷新。

**触发条件：** 你告知"某来源已变更/删除" 或 lint 报告中检测到来源变更

#### 场景 A：来源被更新（内容修改或版本替换）

**步骤：**
1. 读取 `原始资料/` 中的新版本文件
2. 读取 `知识库/来源/` 中对应的摘要页面（旧版本）
3. 输出新旧差异摘要，请用户确认方向（"内容有 X、Y、Z 三处变化，是否重新消化？"）
4. 等待用户确认
5. 更新摘要页面：
   - 保留原有内容，在顶部追加 **"来源已更新"** 标注框
   - 补充新内容，注明 `[Updated YYYY-MM-DD]`
   - 更新 `updated` 字段和 `tags`（保留 `status/reviewed`）
6. 检查所有引用了该来源的知识库页面：
   - 如旧数据已过时 → 添加撤消标注（附日期）
   - 如新数据补充了信息 → 追加内容并注明来源
   - 如新旧数据矛盾 → 保留矛盾标注并注明版本背景
7. 更新 `知识库/index.md` 中该来源的摘要行
8. 在 `知识库/log.md` 记录

**验收标准：**
- 旧版本内容保留（可追溯历史）
- 所有引用过该来源的页面已评估并处理
- 没有信息在未经提示的情况下被静默覆盖

#### 场景 B：来源被重命名

**步骤：**
1. 更新 `知识库/来源/` 摘要页面 frontmatter 中的 `source:` 字段指向新路径
2. 如果摘要页文件名随之调整，更新所有引用该页面的 wiki-link
3. 更新 `知识库/index.md` 中的链接和摘要行
4. 在 `知识库/log.md` 记录

**验收标准：**
- `source:` 字段指向有效的新路径
- 没有断裂的 wiki-link

#### 场景 C：来源被删除

**步骤：**
1. 更新 `知识库/来源/` 摘要页面 frontmatter 中的 `source:` 字段指向新路径
2. 如果摘要页文件名随之调整，更新所有引用该页面的 wiki-link
3. 更新 `知识库/index.md` 中的链接和摘要行
4. 在 `知识库/log.md` 记录

**验收标准：**
- `source:` 字段指向有效的新路径
- 没有断裂的 wiki-link

### 5. Lint — 健康检查

**触发条件：** 用户说"做 lint"或"检查健康"

**检查清单（按严重程度排序处理）：**
1. **断裂链接** — `[[link]]` 的目标页面是否存在
2. **来源变更检测** — 对比 `原始资料/` 与 `知识库/来源/` 的文件清单，标记：
   - 摘要页的 `source:` 指向已不存在的文件
   - 摘要页的 `source:` 指向被重命名的文件
   - 摘要页的 `updated` 日期早于对应来源文件的修改日期（提示可能已更新）
3. **跨页面矛盾** — 搜索"矛盾"、"然而"、"另一种说法"等标记词
4. **孤立页面** — 没有被其他页面引用的页面
5. **缺失概念** — 出现 > 3 次但没有独立页面的概念
6. **标签一致性** — 标签拼写差异（如 `#AI` vs `#artificial-intelligence`）
7. **未处理来源检测** — 扫描 `原始资料/` 中带日期前缀的文件和目录：
   - 无对应摘要页且无 ingest-failed 记录 → 普通未处理
   - 无对应摘要页但 `知识库/log.md` 有 ingest-failed 记录 → ⏳ 因格式阻塞
8. **领域索引同步** — `知识库/领域索引.md` 是否有页面未被收录

**输出报告格式：**

```markdown
## Lint Report — 2026-05-01
### 🔴 紧急（断裂链接）
- `知识库/实体/file.md` 引用了 [[missing-page]] 但该页面不存在
### 🟡 中度（矛盾）
- `知识库/概念/pricing.md` 与 `知识库/来源/article.md` 的价格数据不一致
### 🟢 建议
- 概念 "RAG" 在 7 个页面中出现，建议创建独立页面
```

依次处理每个问题，修改前请用户确认。

---

## 验证矩阵

| 场景 | 预期 | 通过标准 |
|------|------|---------|
| 空目录初始化 | 完整三层结构就绪 | CLAUDE.md 含完整工作流定义 |
| 投递 1 篇文章 | 创建 1-2 页，更新 2-5 页 | index.md + log.md 同步更新 |
| 连续投递 5 篇同主题 | 主题页面逐步深化，矛盾自动标注 | 最终页面引用全部来源 |
| 来源被更新 | 重新消化，对比新旧，增量刷新受影响页面 | 旧内容保留可追溯，新内容以日期区分 |
| 来源被删除 | 标记归档，不删摘要页 | 引用该来源的页面全部标注"基于已删除来源" |
| 综合查询 | 回答引用 3+ 页面，有 wiki-link | 每个事实可追溯来源 |
| 投递 1 篇跨领域文章 | 正确分配多个领域标签，领域索引同步更新 | 标签符合 8 领域分类法，领域索引.md 收录该页面 |
| Lint 检查 | 分层报告，断裂链接归零 | 所有修复经用户确认 |


## 框架机制

### 原始资料组织规则

#### 已确定的规则

- **来源文件命名**：`YYYY-MM-DD_英文描述名.扩展名`，所有格式统一规则，按时间自然排序
- **多文件来源**：涉及多个相关文件时，统一放入 `YYYY-MM-DD_英文描述名/` 子目录，目录作为整体来源处理
  - 内部文件无需日期前缀（从目录名继承），使用 `kebab-case` 命名
  - ingest 时扫描目录下所有可读文件，合并在同一来源中处理
- **附件存放**：配套图片（Web Clipper 截图、文章插图等）统一放入 `原始资料/附件/`
  - 附件本身不触发 ingest（单独丢图片不会启动消化流程）
  - 但在对应 .md 来源被 ingest 时，LLM **会读取附件图片作为上下文**以理解内容
  - 建议按来源组织子目录：`附件/2026-05-03_文章名/图片1.png`
- **图片作为来源 vs 作为配套**：
  - `原始资料/根目录` 下的图片 → 作为独立来源，正常 ingest（LLM 读取图像内容）
  - `原始资料/附件/` 下的图片 → 不作为独立来源，仅在对应来源 ingest 时作为上下文读取
- **知识库引用图片**：不得直接嵌入 `原始资料/` 中的文件（原始资料可能被删除）。ingest 时 LLM 将引用到的图片从 `原始资料/附件/` **复制到 `知识库/媒体/`**，知识库页面引用副本
  - 子目录按来源组织：`知识库/媒体/2026-05-03_文章名/图片1.png`

#### 处理规则

- **可读格式**（.md, .pdf）→ LLM 直接读取，正常 ingest
- **不可读格式**（.pptx, .docx, .xlsx, .mp3, .mp4 等）→ ingest 时执行格式检测，输出报告说明并在 log.md 记录失败日志，**不阻塞用户操作**，由用户决定是否安装工具增强
- 方向：不依赖人工提供文本/转录，由用户按需决定是否通过 Claude Code 安装插件/工具来扩展格式支持

### CLAUDE.md 增长管理

- 以下情况执行拆分：**CLAUDE.md 超过 600 行**
- 拆分的低频内容移入 **`.system/`** 目录（点前缀，Obsidian 默认隐藏）
- 拆分原则：
  - CLAUDE.md 保留：核心规则、页面规范、8 领域分类表（精简版）、Ingest/Query 工作流
  - 移入 `.system/`：Source Sync（A/B/C）、Lint 完整清单、验证矩阵、领域分类完整参考
- 拆分操作由用户主动触发（"CLAUDE.md 太长了，帮我拆分"）

### 知识库目录固化

- 目录结构变更需先在 CLAUDE.md 中更新结构图，再创建/移动文件
- `知识库/` 下的子目录不再新增层级（已有：来源/、概念/、实体/、对比/、媒体/ + index.md、领域索引.md、log.md）

---

*初始化于 2026-05-01，最后更新 2026-05-03*
